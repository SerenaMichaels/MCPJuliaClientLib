# Site-Specific Configuration Template for MCP Client
# This file is for site-specific deployments and should NOT be committed to git
# Copy this to .env.site and configure for your specific deployment site

# Site Information
SITE_NAME=production-client-1
SITE_ENVIRONMENT=production
DEPLOYMENT_ID=client-prod-$(date +%Y%m%d)

# Site-Specific Julia Installation
JULIA_PATH=/opt/julia/bin/julia

# Site-Specific MCP Server Configuration
MCP_SERVER_DIR=/opt/mcp-julia-server

# Site-Specific Database Configuration
POSTGRES_HOST=your-postgres-server.internal
POSTGRES_PORT=5432
POSTGRES_USER=mcp_client_user
POSTGRES_PASSWORD=your_secure_client_password
POSTGRES_DB=mcp_production

# Site-Specific Client Configuration
MCP_CLIENT_TIMEOUT=60
MCP_CLIENT_LOG_LEVEL=INFO
MCP_CLIENT_RETRY_COUNT=5

# Site-Specific Security Settings
MCP_CLIENT_ALLOWED_SERVERS=your-mcp-server.internal:3000
MCP_CLIENT_SSL_ENABLED=true
MCP_CLIENT_SSL_VERIFY=true

# Site-Specific Performance Settings
MCP_CLIENT_MAX_CONNECTIONS=10
MCP_CLIENT_CONNECTION_POOL_SIZE=5
MCP_CLIENT_REQUEST_TIMEOUT=30

# Site-Specific Monitoring
MONITORING_ENABLED=true
METRICS_ENDPOINT=https://your-metrics-server.com/api/client-metrics
ERROR_REPORTING_ENDPOINT=https://your-errors-server.com/api/errors

# Site-Specific Data Directories
CLIENT_DATA_DIR=/opt/mcp-client-data
CLIENT_LOGS_DIR=/var/log/mcp-client
CLIENT_TEMP_DIR=/tmp/mcp-client